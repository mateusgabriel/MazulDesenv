@model IEnumerable<AgendaApp.Entidades.UsuarioPassivo>

<head>
    @{
        ViewBag.Title = "Contatos";
    }
    <script type="text/javascript" src="~/Scripts/usuarioPassivo.js"></script>
    <title>Index</title>
</head>

<body ng-app="usuarioPassivoApp" ng-controller="UsuarioPassivoController">
    <h4>Contatos</h4>

    <div class="fixed-action-btn" style="bottom: 45px; right: 24px;">
        <a class="btn-floating btn-large orange">
            <i class="large material-icons">people</i>
        </a>
        <ul>

            <span ng-if="selection.length > 0">
                <li><a class="btn-floating btn waves-effect waves-light red" ng-click="excluir()"><i class="material-icons">delete</i></a></li>
            </span>
            <span ng-if="selection.length == 1">
                <li><a class="btn-floating btn waves-effect waves-light yellow" ng-click="visualizar()"><i class="material-icons"><i class="material-icons">pageview</i></i></a></li>
            </span>
            <li>
                <a class="btn-floating btn waves-effect waves-light light-blue darken-3" href="#" onclick="location.href='@Url.Action("Inserir", "UsuarioPassivo")'">
                    <i class="material-icons">add</i>
                </a>
            </li>
        </ul>
    </div>

    <table class="responsive-table">
        <thead>
            <tr>
                <th></th>
                <th>Nome</th>
                <th>Email</th>
                <th>Telefone</th>
            </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <input class="filled-in" id="@item.Id" name="contatos" type="checkbox" value="@item.Id" ng-checked="selection.indexOf(@item.Id) > -1" ng-click="toggleSelection(@item.Id)" />
                    <label for="@item.Id"></label>
                </td>
                <td>@Html.DisplayFor(modelItem => item.Nome) @Html.DisplayFor(modelItem => item.Sobrenome) </td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.Telefone)</td>
            </tr>
        }
    </table>
</body>

@*<table class="table-responsive">
        <thead>
            <tr>
                <th>
                    Nome
                </th>
                <th>
                    Endereço
                </th>
                <th>
                    Telefone
                </th>
                <th>
                    Email
                </th>
                <th>
                    Ações
                </th>
            </tr>
        </thead>

        <tbody ng-repeat="item in usuariosPassivos">
            <tr>

                <td><a data-ng-href="@Url.Action("Visualizar", "UsuarioPassivo")/{{item.Id}}">{{item.Nome}} {{item.Sobrenome}}</a></td>
                <td>{{item.Endereco}}</td>
                <td>{{item.Telefone}}</td>
                <td>{{item.Email}}</td>
                <td>
                    <a data-ng-href="@Url.Action("Editar", "UsuarioPassivo")/{{item.Id}}">Editar</a>
                    <a class="modal-trigger" href="#modal1">Excluir</a>
                </td>
            </tr>
        </tbody>
    </table>*@